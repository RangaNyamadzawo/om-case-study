version: "3.9"
services:
  express:
    build:
      context: ./express-app
      dockerfile: Dockerfile
    container_name: express-app
    environment:
      - PORT=4567
    ports:
      - "4567:4567"
    restart: unless-stopped
